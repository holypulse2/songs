{
    "private": true,
    "name": "songs-monorepo",
    "workspaces": ["packages/*"],
    "scripts": {
        "startall": "yarn install && cd packages/frontend && yarn run build && cd ../backend && yarn start"
    }
 }