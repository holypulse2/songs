{
    "private": true,
    "name": "songs-monorepo",
    "workspaces": ["packages/*"],
    "scripts": {
        "start": "yarn install && cd packages/frontend && yarn run build && cd ../backend && yarn start"
    }
 }